#include "main.h"
#include "2-strlen.c"

/**
 * _puts - prints a string, followed by a new line.
 * @str: input string.
 * Return: no return.
 */
void _puts(char *str)
{
	int m = 0;
	
	for (m = 0; *(s + m) != '\0'; m++)
	{
	_putchar(*(str + i));
	}
	_putchar('\n');
}
