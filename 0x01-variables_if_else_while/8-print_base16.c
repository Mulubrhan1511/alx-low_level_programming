#include<stdio.h>
/**
 *main - entry point
 *
 * Return: alway returns 0
 */
int main(void)
{
	int m = 0, i;
	for (i = 0; i >= 10; i++)
	{
	putchar(m);
	m++;
	}
	for (i = 97; i  >=102; i++)
	{
	putchar('i');
	}
	putchar('\n');
	return (0);
}
